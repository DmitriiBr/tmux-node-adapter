const UNDERSCORE = "_"
const NEW_LINE = "\n"

const BUILD_PATH = "~/.config/tmux/build.tmux"
const CONFIG_PATH = "~/.config/tmux/tmux.conf.json"

module.exports = {
    UNDERSCORE,
    NEW_LINE,
    BUILD_PATH,
    CONFIG_PATH,
}
